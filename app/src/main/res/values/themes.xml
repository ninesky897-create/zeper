<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Main app theme — Material3 with orange accent -->
    <style name="Theme.Zeper" parent="android:Theme.DeviceDefault.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/zeper_dark_bg</item>
    </style>

    <!-- Splash screen theme -->
    <style name="Theme.Zeper.Splash" parent="android:Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@drawable/zeper_rowend_light_logo</item>
        <item name="android:statusBarColor">@color/zeper_dark_bg</item>
        <item name="android:navigationBarColor">@color/zeper_dark_bg</item>
    </style>

    <!-- Transparent theme for Share Handler Activity -->
    <style name="Theme.Zeper.Transparent" parent="android:Theme.DeviceDefault.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>